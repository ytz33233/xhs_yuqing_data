# 看板逻辑检查清单

## 已修复问题

### 1. 2026-04-28.json 语法错误
- **问题**：3处中文引号未转义（"金龙献瑞"、"有流量无留量"、"薅羊毛"），导致 JSON 解析失败
- **影响**：范围查询包含 04-28 时该文件被跳过，数据丢失
- **修复**：转义为 \""

### 2. file:// 协议下范围查询为空
- **问题**：浏览器安全策略阻止 fetch 本地 JSON
- **修复**：`loadByRange()` 检测 `file://` 协议，fetch 全部失败时回退到内嵌 fallbackData

### 3. 趋势图日期匹配逻辑错误
- **问题**：`computeTrend` 用 `r.date`（舆情原始发布日期，如 2026-01-27）匹配查询范围日期（如 2026-05-02），永远不匹配
- **修复**：改用 `fetchTime` 的日期部分匹配，反映「哪天监测到这条舆情」

### 4. loadLatest fallback 日期硬编码
- **问题**：fallback 时日期框硬编码为 2026-04-28，与 fallbackData 实际日期（05-08）不一致
- **修复**：使用 `fallbackData.reportDate` 动态设置

### 5. 详情弹窗时间线硬编码
- **问题**：「当前状态」时间硬编码为 2026-04-28
- **修复**：使用 `loadedData.reportDate` 动态显示

### 6. KPI 标签误导
- **问题**：「今日新增舆情」在范围查询模式下语义错误
- **修复**：改为「舆情总数」

### 7. 趋势图标题固定
- **问题**：「舆情趋势（近7日）」与可能非7天的查询范围不符
- **修复**：改为「舆情趋势」

### 8. applyFilters / resetFilters 影响全局图表
- **问题**：筛选表格时联动重绘 KPI、饼图、来源图，但它们读取的是 loadedData（总体数据），不会随筛选变化，造成「筛选后图表不变」的困惑
- **修复**：筛选/重置仅刷新表格，KPI 和图表始终反映当前加载范围的总体数据

## 当前数据流

```
初始化 → loadLatest()
  ├─ 能连服务器 → 找最新有数据日期 → loadByRange(最新-6天 ~ 最新)
  └─ file:// 或全部失败 → fallbackData → 单日期显示

用户点「查询」 → loadByRange(start, end)
  ├─ 遍历范围内每天，fetch data/YYYY-MM-DD.json
  ├─ file:// 全部失败 → fallbackData.records
  └─ 合并 → 去重 → 计算统计 → renderAll()

用户筛选表格 → applyFilters()
  └─ 仅过滤表格数据，KPI/图表保持当前范围总体

用户重置筛选 → resetFilters()
  └─ 恢复全部表格数据，KPI/图表不变
```

## 文件状态

| 文件 | 状态 |
|------|------|
| `data/2026-04-28.json` | 有效，6条记录 |
| `data/2026-05-07.json` | 有效，0条记录 |
| `data/2026-05-08.json` | 有效，10条记录 |
| `dashboard.html` | JS 语法通过验证 |

## 使用逻辑验证

| 场景 | 预期行为 |
|------|---------|
| 直接打开 HTML | 自动加载 fallbackData（10条），日期显示 05-08 |
| 修改范围后点查询 | 范围内有 JSON → 加载合并；无 JSON + file:// → fallback |
| 筛选关键词/情感/来源/时效 | 仅表格变化，KPI和图表保持总体 |
| 点重置 | 表格恢复全部，筛选框清空 |
| 点击详情 | 弹窗显示完整信息，时间线日期动态 |
| 范围跨多天 | 趋势图按 `fetchTime` 分布显示每天收录数量 |
